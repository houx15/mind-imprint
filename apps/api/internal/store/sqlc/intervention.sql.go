// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: intervention.sql

package sqlc

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const insertIntervention = `-- name: InsertIntervention :one
INSERT INTO intervention (project_id, card_instance_id, type, anchor, criterion, body, level, output_check_verdict)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
RETURNING id, project_id, card_instance_id, type, anchor, criterion, body, level, output_check_verdict, created_at
`

type InsertInterventionParams struct {
	ProjectID          uuid.UUID   `json:"project_id"`
	CardInstanceID     pgtype.UUID `json:"card_instance_id"`
	Type               string      `json:"type"`
	Anchor             []byte      `json:"anchor"`
	Criterion          *string     `json:"criterion"`
	Body               string      `json:"body"`
	Level              *string     `json:"level"`
	OutputCheckVerdict *string     `json:"output_check_verdict"`
}

func (q *Queries) InsertIntervention(ctx context.Context, arg InsertInterventionParams) (Intervention, error) {
	row := q.db.QueryRow(ctx, insertIntervention,
		arg.ProjectID,
		arg.CardInstanceID,
		arg.Type,
		arg.Anchor,
		arg.Criterion,
		arg.Body,
		arg.Level,
		arg.OutputCheckVerdict,
	)
	var i Intervention
	err := row.Scan(
		&i.ID,
		&i.ProjectID,
		&i.CardInstanceID,
		&i.Type,
		&i.Anchor,
		&i.Criterion,
		&i.Body,
		&i.Level,
		&i.OutputCheckVerdict,
		&i.CreatedAt,
	)
	return i, err
}

const listInterventionsByProject = `-- name: ListInterventionsByProject :many
SELECT id, project_id, card_instance_id, type, anchor, criterion, body, level, output_check_verdict, created_at FROM intervention
WHERE project_id = $1
ORDER BY created_at, id
`

func (q *Queries) ListInterventionsByProject(ctx context.Context, projectID uuid.UUID) ([]Intervention, error) {
	rows, err := q.db.Query(ctx, listInterventionsByProject, projectID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Intervention
	for rows.Next() {
		var i Intervention
		if err := rows.Scan(
			&i.ID,
			&i.ProjectID,
			&i.CardInstanceID,
			&i.Type,
			&i.Anchor,
			&i.Criterion,
			&i.Body,
			&i.Level,
			&i.OutputCheckVerdict,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
