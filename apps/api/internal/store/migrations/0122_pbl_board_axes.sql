-- +goose Up
-- 便签板的坐标视图开着没有。
--
-- 🚨 板上的 x/y 一直存着，但它只是「她把纸放在了哪儿」——一个没有意义的位置。
-- 打开坐标视图之后，同一个位置变成一句**判断**：这条我有多确定、它有多要紧。
-- 拖动这个她本来就在做的动作，因此变成一条可以回灌给印记的信号。
--
-- 为什么要存这一位：象限只有在她真的按坐标摆过之后才说明问题。没开过坐标视图
-- 的板子，x/y 是 boardSpot() 派的座位，把它读成「她认为这条不要紧」是在编造。
-- 所以印记那边要先知道这块板是不是按坐标摆的。
--
-- 轴本身写死在前端（确定度 × 要紧程度），不进库：换一对轴是产品决定，不是每个
-- 项目各自的数据；真要按项目定制，那时候再加一列也不迟。
ALTER TABLE pbl_project ADD COLUMN board_axes boolean NOT NULL DEFAULT false;

-- +goose Down
ALTER TABLE pbl_project DROP COLUMN board_axes;
