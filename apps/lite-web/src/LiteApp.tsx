/**
 * 轻量版 shell 占位（Task 9 脚手架）。房间组件（阅读室 / 写作室）将从
 * apps/web 源码原样引入 —— lite 只提供外壳与自己的 API 客户端。
 */
export function LiteApp() {
  return (
    <div className="min-h-screen bg-mk-paper text-mk-ink">
      <div className="p-8">思维印记 · 轻量版</div>
    </div>
  );
}
