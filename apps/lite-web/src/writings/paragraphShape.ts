import type { OutlineKind } from "./outlineKind";

/**
 * 一段里的几步 —— 段落那一步的引导框顶上那一节。
 *
 * # 为什么是这个（同事 2026-09-20 的意见 5）
 *
 *	「到了分段写作的界面，需要组织的内容为**一个段落内的文本结构**。
 *	  ……写作引导：语言还是有点笨，几种写法不太规范，我找了一些论证结构的
 *	  思维导图和教辅，可以参考一下这些论证方法规范一下语言。」
 *
 * 引导框原来那一节叫「常见的几种写法」，摆的是几个**方法名**（举个例子、
 * 比一比……）。但她站在一段空白面前要的不是「有哪几种写法」，是
 * **这一段先写什么、再写什么**。方法名回答的是另一个问题。
 *
 * # R4（2026-09-21）：换成讲义的五句型
 *
 * 依据从「那页教辅的分析段内层次」换成了产品负责人拿来的那位语文老师的
 * `docs/reference/writing-teaching/议论文写作汇总（五）讲义.md`。
 * 主体段原来是四步（分论点句 / 论据 / 分析 / 回扣），讲义是五句：
 *
 *	观点句 → 阐释句 → 材料句 → 分析句 → 结论句
 *
 * 少的那一句是**阐释句**，而它恰好是学生最常跳过的一句：观点句写完直接跳到
 * 例子，抽象的主张和具体的事之间没有桥，读者得自己搭。
 *
 * 「分析」那一格也换了说法。R2 上线那天模型自己说的是「例子讲完就结束了，
 * 还差一句把它和主张连起来的话」—— 它认得出缺什么，说不出那一句叫什么。
 * 讲义给了三个名字（因果 / 假设 / 归纳），三条都在方法库里带着句式。
 *
 * 记叙文那四种（场景 / 细节 / 转折 / 感悟）是 R4 新开的，来源是另外两份讲义。
 *
 * # 🚨 这一节是确定性渲染的，不花模型调用
 *
 * 按 kind 查这张表，和卡片派生是同一个路子。模型那一份只管 `job` /
 * `method_ids` / `questions` —— 一段的骨架是固定的知识，每次让模型重说一遍
 * 既费钱又会漂（同事的意见 7：「每一次刷新就会变成新的东西」）。
 *
 * 🚨 `apps/api/internal/api/writing_sentence.go` 是它的 Go 孪生，
 * 陪练判「这一段缺哪一句」时用的是那一份。两边的步数、次序、用词必须一致。
 */
// 🚨 这些字**直接渲染成纯文本**，不过 markdown。写 `**强调**` 会原样印出两个
// 星号 —— 2026-09-20 的看图台截图里就是这么发现的。要强调就换词，不加记号。
export type ParagraphStep = { label: string; hint: string };

const OPENING: ParagraphStep[] = [
  { label: "引出话题", hint: "一句话让读者知道为什么值得谈这件事。" },
  { label: "亮出中心论点", hint: "这篇要证明的那一句，直接说出来。" },
  { label: "交代分几条讲", hint: "一句话说清后面分几条。不必举例——例子在后面的段里。" },
];

const SHAPES: Record<string, ParagraphStep[]> = {
  opening: OPENING,
  thesis: OPENING,
  // 讲义（五）的主体段五句型。
  point: [
    { label: "观点句", hint: "这一段要证的那一句，放在段首。里面要有中心论点的关键词，读者才知道它扣着题。" },
    { label: "阐释句", hint: "把观点句里那个抽象的词说开一层，再去找例子。这一句最常被跳过，跳过了主张和例子之间就没有桥。" },
    { label: "材料句", hint: "谁、做了什么、结果怎么样。叙述要简明——这一段是论证不是记叙，材料占的篇幅别超过一半。" },
    { label: "分析句", hint: "这件事凭什么证明观点句。三种写法：只摆了现象就追原因，已经有结论就问假如，列了好几个例子就找共性。" },
    { label: "结论句", hint: "回到段首那一句，扣住关键词，说清这一条把中心论点推进了哪一步。" },
  ],
  counter: [
    { label: "反方最强的那一点", hint: "写它真正有力的版本，不是一个好打的版本。" },
    { label: "承认它成立的地方", hint: "承认得越具体，后面的转折越有力。" },
    { label: "指出它的适用范围", hint: "它在什么情况下才成立——范围之外就轮到你的结论。" },
  ],
  rebuttal: [
    { label: "接住对方那一点", hint: "先复述你要回应的到底是哪一句。" },
    { label: "指出它的适用范围", hint: "不是说它全错，是说它管不到这里。" },
    { label: "回到自己的结论", hint: "一句话说清为什么你的说法仍然站得住。" },
  ],
  closing: [
    { label: "回到中心论点", hint: "回到开头那一句，但不要原样再说一遍。" },
    { label: "说得比开头更准", hint: "用全文证到的东西，把那句话重新说一次。四种收法：回应开头、引一句名言、把几条收成一句、用一组句子收住。" },
    { label: "落到一件具体的事上", hint: "一个可执行的建议，比一句口号有力。讲义的硬话：结尾不超过两百字。" },
  ],

  // —— 记叙文（R4）——
  scene: [
    { label: "交代时间和地点", hint: "哪一天、在哪儿、当时在做什么。读者要先站得住，才看得见后面的事。" },
    { label: "把这件事写具体", hint: "动作、神态、说过的话，挑和主题相关的写。动词要准：「弓着身子蹒跚着走来」和「走过来」是两件事。" },
    { label: "写你当时的反应", hint: "从惊讶到明白，一点点转过来。写心理变化，不要直接写一句「我很感动」。" },
  ],
  detail: [
    { label: "选一个角度", hint: "人物的动作神态、周围的环境、还是一件东西。一次写一个，写透。" },
    { label: "换准的动词和修饰词", hint: "把「走」「拿」「看」换成只有这个人在这一刻才会做的那个动作。" },
    { label: "检查这三条", hint: "是真的发生过吗，是不是只有他才会这样，换个人还成立吗。三条都过了才留下。" },
  ],
  turn: [
    { label: "过渡一句", hint: "「日子就这样一天天过着，直到有一天」。先铺平，转折才不突兀。" },
    { label: "让环境替你说话", hint: "天气、时辰、周围的声音。环境越难，他做的那件事越见分量。" },
    { label: "写触发的那一下", hint: "一个具体的动作或一句话，让你突然发现之前想错了。这一下是全篇最要紧的地方。" },
  ],
  feeling: [
    { label: "说出新的认识", hint: "这件事之后，你对这个人、这件事的看法变成了什么。" },
    { label: "回到开头", hint: "呼应开头写的那份不满，让读者看见你确实变了。" },
    { label: "落到以后", hint: "往后你打算怎么做。一句就够，不要喊口号。" },
  ],
};

/**
 * 这一种块的段内结构。没有对应的（自由段落、论据）返回空数组 ——
 * 那时候这一节整个不渲染，而不是摆一份不适用的步骤。
 */
export function paragraphShapeOf(kind: OutlineKind | string): ParagraphStep[] {
  return SHAPES[kind] ?? [];
}
