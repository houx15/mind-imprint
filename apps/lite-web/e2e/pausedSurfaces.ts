import { test } from "@playwright/test";

/**
 * 项目 / 我的主页 这两个面，2026-09-21 起先藏起来了。
 *
 * 产品负责人原话：
 *
 *   「hide 项目 and 我的主页 tabs for lite version now because the two are
 *     not finished yet」
 *
 * 底栏那两格、首页那一行入口、「最近在学」里的项目条都藏了；**路由没动**，
 * 手上正做着一个项目的人输网址仍然进得去，库里一行都没改。
 *
 * # 为什么连带把这几条走查停掉
 *
 * 它们一路钉着这两个面上每一屏的文案，而那些字正跟着「把它做完」一起改。
 * 2026-09-21 全量跑线上，这一族全红，红的原因**没有一条**和这次改动有关，
 * 全是文案漂移（例：projects-walk 钉的「最近想做点什么」这个标题已经不在了）。
 *
 * 一条红着没人管的走查，会把真的问题盖住 —— 今天修分级阅读那一族，
 * 就是从一堆「反正它一直红着」里把三个真缺陷翻出来的。所以宁可明写「停了，
 * 因为这一面还在做」，也不要让它继续红成背景噪音。
 *
 * 🚨 这两个面做完之后，把这个调用删掉，照新的界面重跑一遍 ——
 * 别直接把 skip 去掉就当它绿了。
 */
export const PAUSED_REASON =
  "项目 / 我的主页 这两个面还没做完（产品负责人 2026-09-21），底栏已藏；" +
  "这几条走查钉的文案正跟着改，先停，做完之后照新界面重写。";

/** 在文件顶上调一次，这个文件里的每条走查都会被跳过并说明原因。 */
export function skipWhilePaused(): void {
  test.beforeEach(() => {
    test.skip(true, PAUSED_REASON);
  });
}
