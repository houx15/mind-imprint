# 教学素材里那几处冲突，怎么定的（2026-09-24）

`docs/reference/writing-teaching/distilled/README.md` 最后一节列了几处「来源内部
互相矛盾，要产品负责人选边」。这份文件把它们定下来，并写清**定错了会怎样、
怎么改回去** —— 每一条都是可撤的。

产品负责人 2026-09-24 的指示是「go ahead」，所以这几条由工程先定、标出来，
不停下来等。下面每一条的「如果你不同意」那行就是撤回的入口。

---

## 1. 词数超标：扣分，不压档 —— 这一条原来不是冲突

**定：** 词数是**一处独立的扣分**（应用文 80 词左右，`<60` 或 `>100` 扣 2 分）；
**要点缺失**才影响档次上限（缺 1 个通常不高于四档，缺一半以上不高于三档）。

**为什么这不是选边：** 把两份 rubric 摊开看，它们说的根本是两件事 ——

- `[C]references/rubric.md` 与 `[G]` 两份都写着「词数 <60 或 >100 → 总分减 2 分」；
- 「档次上限受限」那句话出自**要点遗漏**那一条，和词数无关。

我原来把这两句并排读成了一处冲突。它不是。两条各自照原样实现即可。

**已落地：** `WritingPlanSkeletonLetterEN` 里写的是「明显偏少或者明显超出都要
扣分」，并据此禁掉「再补一个理由把这一段写厚」。

---

## 2. 结尾加一句谚语：按学段分，而且真正的判据是「接不接得上」

**定：**

- **初中**：结尾加一句谚语或感叹句算**升格建议**，照 `[W]` 的 rubric 给。
- **高中／高考**：不主动建议；只有当它**与上下文脱节**时才标出来，说明理由。

**为什么这是解释而不是折中：** 高考那份 rubric 的原话不是「不许用谚语」，是
「低水平习作中出现与水平不匹配的『背模板句』要标注出来，并提示『与上下文
脱节、有背诵痕迹』」。**被判的是脱节，不是谚语。** 于是两份 rubric 的真正
差别只剩「初中要不要主动推荐它」，而那正是学段的差别。

**为什么落得下去：** 学段这条轴已经有了 —— `guidance.Key.Grade` 加
`classes.grade`，junior / senior 分得开。不需要新机制。

**如果你不同意：** 把初中那一侧关掉即可（两种学段都只做「脱节才标」）。
代价是初中学生少一条他们的 rubric 认的升格路径。

---

## 3. R10「讨论反例与适用条件」 vs G31「500 字任务不强制多条理由」

**定：按目标篇幅决定这一步是必经还是只提一句** —— 这不是我的发明，是产品
负责人自己的 **G24**「结合任务确定理由数量与证据要求」。

- 短任务（中考量级的短文 / 短文章）：不单独走这一步。要求落在**那一条理由
  自己身上** —— 说清它在什么情况下成立，一句话，不是一段。
- 长任务：照 R10 原样，「讨论反例与适用条件」是必经的一步。

**为什么这样切：** R10 要防的是「把一个看法说成放之四海而皆准」；G31 要防的是
「短任务被塞进撑不住的结构」。把「适用条件」挂到那条理由上，两件事同时成立，
而且不多出一个段落。

**如果你不同意：** 把门槛调低（让更多任务走完整的那一步），或者干脆让它无条件
必经。代价分别是短文变啰嗦、或者回到 G31 点名的那种过度要求。

**注意** 这一条只说「走不走这一步」。和它同形的另一个毛病 ——
放学生走的那条线写死成「两条分论点」，不看目标篇幅 —— 2026-09-12 已经修好了
（`writing_plan_state.go` 按目标字数算，`writing_plan_invite_test.go` 用 800 字
和 3000 字两条钉着）。

---

## 4. 🚨 文言文字词那张闭表：九类里有五类是**补**出来的，不是老师写的

这一条不是冲突，是**出处**问题，而且它已经在线上跑着，所以单独列出来。

`reading_block.go` 的字词卡要求模型从一张闭表里选类别：

> 通假字 / 古今异义 / 一词多义 / 词类活用 / 偏义复词 / 专有名词 / 典故 / 虚词 / 实词

按 `distilled/classical-reading.md` 第 63 行的标注：

| 类别 | 出处 |
|---|---|
| 通假字、古今异义、专有名词、典故 | `[源]` —— 老师那份讲义第二层逐字列出 |
| 一词多义、词类活用、偏义复词、虚词 | `[补]` —— 按中学通行判据补的 |
| 实词 | 工程加的（2026-09-24 线上走查：「蓑」九类一条都不沾，模型于是**编**了一条理由） |

**定：留着照用。** 这五类是中学语文的通行分类，不是生造的；而且去掉它们，
一个「怒而飞」的「怒」就没有地方可落，模型只会把它塞进最近的一类或者自己编 ——
线上已经见过这种编法（「前有『舟』动词词，『蓑』修饰『笠』」，两处都是错的）。

**但这是工程的判断，不是你的教研组的判断。** 请过一眼这张表，尤其是
「偏义复词」这一类值不值得让初中生分辨。

**如果你不同意：** 闭表在 `reading_block.go` 的 `readingShapedSuffix["hanwords"]`
一处，改那一行即可；`reading_article_internal_test.go` 里钉着其中四个词，
跟着改。

---

## 5. 🚨 R3 / R5 / R13 动不了 —— 它们撞上你自己下过的一条禁令

这三条都落在**议论文的阅读体验**上：

- **R3** 「…→ 拼出全文结构 → 按文章类型精读 …」：议论文那两套读法里没有
  「拼出全文结构」这一步，通读完直接进精读。
- **R5** 「先让学生说清这一部分讲了什么，再进入细节」：今天学生说一句
  「读完了」就能过。
- **R13** 「可以 ai 示范一段，学生自己分析一段」：示范额度完全没有。

2026-09-24 我把 R3 和 R13 都做出来了（议论文多一步「拼出全文结构」，
议论文多一段带读说明装 R13），然后测试红了四条，红的理由逐字写着：

> 🚨 议论文的三套读法，步骤一个都不许动。
> 「don't bother the current experience of argument papers」
>
> 🚨 议论文那条路一个字节都不变 —— 这是 2026-09-17 定的边界。

**所以两处改动都撤回了。** 这不是测试过时，是测试在替你看门：R3/R5/R13 想改的
正是那条禁令保护的东西。「把列出来的都做完」是一句一般的话，
盖不过一条专门针对议论文阅读的禁令。

**要它们就说一声**，三条都是半天的事：

| | 动到哪里 | 代价 |
|---|---|---|
| R3 | 议论文两套读法各多一步（reflect·拼出全文结构，指向已有的论证图） | 议论文的步数从 8 变 9 |
| R5 | 通读步加一道闸：她这一部分的概括没落下来之前不进细节 | 她想快进的时候会被拦 |
| R13 | 议论文多一段带读说明：示范一段之后每段先请她说 | 印记在议论文上话变少 |

R13 我不建议做成服务端的计数器：要数「示范过几段」得把印记过去的话分类成
「这是示范 / 不是示范」，那是一个模糊分类器，正是 memory
`detector-must-target-the-real-failure` 里记的那种「判据盯着影子」。
规矩写进提示词、判据交给真模型那一侧就够了。

## 顺带核过的两条：R4 和 R11 **已经是做好的**

交给我的那张「未处理」清单不可靠，逐条核过之后：

- **R4**「每次通读以 2–4 段为起点」——`ReadingPlanSystem` 里逐字写着
  「每部分通常 2 到 4 段，最多 6 部分」。已实现。
- **R11**「内容轴 + 组织架构轴 + 框架图」——2026-09-24 建整篇那一层时
  `argument_map` 就是按两轴写的：label 走内容轴（中心论点/分论点/正面论据/
  反面论据/分析段/让步/结论），spine 走组织轴（递进/对照/总分/分总/总分总/
  并列）并且要求说明凭什么这么判。已实现。

## 6. 查过、结论是「不用改」的三处

**结构解析在《江雪》上一闪而过。** 产品负责人 2026-09-23 报的。查完之后两半各有
答案，都不是「修了一个 bug」：

- 「如果做完了结构解析，它就不能再点了吗？」—— **不是。** 开过的工具照样能点
  （`BlockToolbar` 里只有 `disabled={busyTool !== null}`，开过只是换个样式
  表示「这是你开过的」）。
- 那个一闪而过的图标 —— **现在诗上根本不会出现了。** 2026-09-24 起
  `structure` 和 `examples` 按体裁从诗词的工具单里撤掉了（十个字里没有层次
  可拆，也没有数据可举）。线上实测《江雪》的工具单：classical_translate /
  classical_word / poem_images / rhetoric / questions / imitate / poem_shape，
  没有 structure。

触发它的那件工具已经不在那条路上了，所以没有继续追那一帧。
要是在别的体裁上再看到，请告诉我是哪一篇 —— 那会是另一个形状的毛病。

**一首诗按「两句一段」粘进来时，卡片上是两行不是四行。** 这是照她粘的样子
画的，不是 bug：`SplitBlocks` 只在空行处切段。一句一行粘进来就是四行
（2026-09-24 修好的正是这个 —— 原来四句会连成一行）。
要在粘贴时按句重切，会动到标注的锚点（她之前存下的每一条批注都会错位），
那需要单独想清楚，不顺手做。

**书信那一档没有加线上走查。** 写作室这个房间本身是不分文体的 —— 文体只换
提示词的内容，不换页面。所以一条书信的 e2e 走查证明的是「房间还在」，
而「这一篇按书信在教」已经由真模型那两条实测钉住了
（`writing_letter_en_live_test.go`）。按判据落在真正会坏的那一层这条原则，
没有加。

## 还留在桌上的

读后续写和概要写作 2026-09-24 当天做完了（各自的文体、块、提示词、判前输入
检查、真模型实测都在），所以这一栏现在只剩两样：

1. **上面第 5 节那三条**（R3 / R5 / R13），等你一句话。
2. **书信和散文仍在拿议论文那份「根据观点选择材料」**。SlotMaterial 原来没有
   文体这条轴；读后续写和概要写作已经各给了自己的一份（那两档拿议论文的是
   **错的**），书信和散文没有一起改 —— 它们至少「找材料」这件事本身成立，
   改它们要各写一份新文本，是另一件事。
