# main 新提交文案复查

2026-09-15。检查范围：上轮 `b6b18cd3` 到当前本地 main `b846f687` 的 32 个提交（237 个文件的净变更）。已获取远端 main `6669d234`，确认它是本地 main 的祖先。主要检查新增或修改的 Lite 教师端、学习首页、阅读、探索、项目工具、兴趣树、报告以及共享 Web / course-renderer 展示代码。

主工作区有正在进行的 PBL 重构，本轮没有导入或修改那些未提交文件。修改保存在独立分支 `codex/copy-followup-0915`。没有合并回 main、推送或部署。

## 已修复

| 原有展示 | 修改 | 原因 |
|---|---|---|
| 轮换、确认轮换 | 更换邀请码、确认更换；明确旧邀请码立即失效 | 操作名称缺少对象，用户难以判断后果 |
| 阅读 1/3、写作 0/1 等 | 保留数量，增加“已完成 / 总数”和平台内时长说明 | 避免误读成成绩或比例 |
| 带读走完了 | 本轮阅读引导已结束 | 状态直接、准确；有跳过任务也不宣称全部完成 |
| 整段工具、提纲节点、归组、整合／合并混用 | 该段的阅读工具、提纲条目、添加到分组、统一用“合并” | 说明操作对象，减少术语和名称漂移 |
| 没做完的不算——只数你真的做出来的东西 | 已完成的阅读、写作和已发布项目的总数 | 去除指责式口吻，保留统计口径 |
| 完成一次一定多一个词、需要几秒 | 根据已完成学习记录更新、整理中 | 不承诺每次必增关键词或固定等待时间 |
| 今日发现页面仍称星图，重试次数耗尽提示生硬 | 统一“今日发现”；说明今日重试次数已用完 | 页面名称一致，给出明确下一步 |
| 英文目标 500 字 | 英文显示 500 词，中文保留字 | 单位准确 |
| 工具 key、原始 JSON、阅读透镜字段 key | 工具目录中文名称、带标签的摘要、共享卡 spec 的字段标签 | 用户先看到可读信息；完整记录仍可展开 |
| 课程 slug、历史 structureKey 直接作标题 | 主展示改为课程学习记录；标识放在展开详情中 | 技术标识不再充当面向教师的标题 |

教师端工具名称由后台既有 `pbl.LookupTool` 目录提供，增加可选展示字段 `label`；原 key、状态、结果和权限不变。未知工具回退原名称。未建立第二份工具目录。

摘要只解释当前已知字段，保留零值、否定布尔值及学生原文；未知字段和嵌套对象保存在完整记录，不擅自推测含义。阅读透镜标签直接来自共享卡 registry。没有运行 HTML 或将学生内容改写为 AI 文字。

## Prompt 边界

检查到的已提交变更没有修改 AI prompt。报告生成函数的新增变化是是否允许第二阶段调用的控制入口，并非 prompt 文字变化。本轮没有修改模型、prompt、评分、推进条件，也没有执行收费模型实验。此前 Flash 的结果不冒充本轮新增测试。

## 验证

- Lite 类型检查通过。
- Lite 全量 72 个测试文件、698 项测试通过。新增 6 项摘要数据边界测试；保留教师／学生称呼区别测试。已有阅读状态测试同步新文案，仍检查完成后不再指向当前任务。
- Lite 构建通过，保留既有大 chunk 提示。工作区另一个应用声明 Node 22，本机 Node 20；本轮未改其环境。
- Go `TestLiteTeacher` 接口回归通过（临时 PostgreSQL，不使用生产数据）。
- 真实浏览器检查 ClassPage、ItemPage、StepIndicator：班级邀请码确认说明、项目摘要、390px 手机宽度及展开完整记录。展开后原始 ID 仍在，页面没有横向溢出。
- 浏览器用一次性组件预览和合成 API 响应；不是登录后的全流程验收。兴趣树截图仅呈现实际 copy 和阅读状态组件，不代表整棵树的布局回归。预览已清理。

截图：[班级说明](browser/class.png)、[手机项目摘要](browser/output-mobile.png)、[手机完整记录](browser/expanded-mobile.png)、[学生文案及阅读结束状态](browser/student-mobile.png)。

## 限制

当前数据没有课程标题，只提供 slug，因此暂以“课程学习记录”展示并保留可展开的标识；没有凭标识编造课程名称。未知工具或未知结果字段仍可能需要查看完整记录。这是后续各工具扩展展示模型时的工作，不通过删除原始内容来掩盖。此次检查针对指定提交区间，不包含其他任务正在修改的未提交页面，也不重新宣称全库旧文案均已达标。
