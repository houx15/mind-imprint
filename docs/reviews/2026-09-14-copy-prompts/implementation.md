# 文案与 coach 修改执行记录

用户于 2026-09-14 同意审阅建议并授权实施，要求独立分支，稍后再合并。

- 分支：`codex/coach-copy-clarity`
- 独立工作目录：`/private/tmp/mind-imprint-coach-copy-clarity`
- 基线：`5f6a1965`
- 不修改 main 或其他工作目录，不合并、不发布。

## 范围与顺序

1. 保存相同场景的原版模型请求与输出；真实模型调用只使用合成教学案例，不连接生产数据库。
2. 修改审阅表中的静态标签/说明、共享卡片与术语；保持 ID、枚举、字段、教学引文不变。
3. 修正 PBL、阅读、写作、自由/项目/课程陪练、审阅的冲突语气要求；保留输出契约与业务校验。修订评测只澄清知识讲解/提示/代写边界，旧新版按同一标准比较。
4. 真实模型输出经生产解析器验证；运行现有逻辑、handler、状态回归，真浏览器核验呈现。
5. 只提交有验证支持的改动，并列出未覆盖/不能并入的内容。

## 写作流程一致性校验（实施前）

已逐节对照 `docs/2026-08-09-all-statuses.md`：

- Beginning：保留引入与用户点击开始，不新增关卡。
- Research framework：仍由学生表达目标/缘由等，保留记录确认、提问卡条件及计划生成条件。
- Project Management：保留生成后说明、返回时回顾和继续入口。
- Writing proposal：保留自行撰写、不同题目的示例、批注/跳过/完成流程；仅澄清说明方式。
- Reading：保留研究目标和原文定位；概念讲解不自动算练习完成。
- Writing paper：保留原文归属、论据和推理检查、学生自行修改及完成信号。
- Review：基于实际过程描述，不把正常求助推断成代写；不改变评估触发。

本批不改阶段枚举、路由、状态机、自动完成条件或 DB schema。新增功能/放宽完成标准不属于本次语气调整。

## 并行工作边界

`apps/site-v2` 当前只存在于 main 的未跟踪工作中。独立分支不收录整份其他同事的未提交站点；其课程文案同步项以单独补丁交付，供用户合并站点工作后应用。

## 最终范围调整

阅读 coach 候选的推进状态实测未通过，运行代码与其文案快照测试已恢复基线，仅保留候选补丁和失败证据。其他功能验证及尚未解决的文风问题见 README.md。浏览器验证为实际组件预览，未执行登录后全流程。
