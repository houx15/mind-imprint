# 个人主页与项目教练迭代

日期：2026-09-22。基线：origin/main `b89fd0e6`。开发分支：`codex/pbl-homepage-coach`。

## 本轮产品边界

个人主页是学生介绍自己、选择并分享平台成果的独立页面，不需要先完成 persona 或主页项目。提供三种有结构区别的版式、六套配色、字体选择、写作与阅读的展示方式、内容选择与排序。预览与访客页面复用同一渲染器。保存草稿与发布分开，旧站点与旧项目历史保留，明确发布新版后才替换公开展示。

项目是 AI 项目教练：通过对话讨论目标与计划，用计划表呈现步骤、学生和 AI 的分工、预期交付物；学生可提交每一步的成果和说明。学生可以在外部 coding agent 或其他工具里制作，不要求导入完整对话、安装 skill 或往返同步。已有 persona、board 等思考工具按需要使用。

后续互动课程会讲解 agent、skill、coding 和 AI 协作。课程尚未开发，本轮不创建假课程或将课程作为项目推进门槛。现有真实课程功能保留。

## 实施顺序

1. 独立主页数据与发布快照，验证归属、选择范围、撤销和并发冲突。
2. 主页编辑器及共用展示组件，验证多种版式、配色和移动端预览。
3. 解除主页发布对项目创建的限制，补齐逐步骤交付物提交与回灌。
4. 调整项目教练提示词，验证计划、学生与 AI 分工、外部制作场景。

每个独立完成并验证的部分单独 commit；不将全部工作累积到最后。不推送、不部署。

## 验收

- 新学生不需要主页项目即可编辑主页、保存、重载；未发布草稿不会改变公开页面。
- 版式、色调、字体和展示方式选择在预览与公开页面一致，窄屏可用。
- 只有学生主动选择的成果出现在主页；阅读/写作仅从已公开作品中选择，撤销后不再展示。
- 旧主页默认保持原状，切换新版是明确发布操作。
- 无主页的学生可以创建项目；计划保留学生与 AI 分工，每步支持交付物记录。
- 真实模型验证与离线逻辑测试分开报告；未测试的行为明确列出。

## 视觉与图片的最终范围

主页依次为全屏 hero、个人介绍、作品。首图完整铺满屏幕，叠加深色遮罩与简洁文字；欢迎标题、介绍与图片可编辑，默认使用清晰无衬线字，移除本轮被否定的装饰字体入口。可选择云朵、月夜、晴空、机械与极简视觉。极简允许无图片。个人介绍支持照片周围关键词或常规名字简介。

作品支持分类、时间轴、星球、词云、贡献日历和列表。日历仅以所选真实作品完成日期计数；没有完成日期的记录另列，不推测日期或填充学习活动。

首图/头像支持站内 draw 模型生成和已有 OSS 上传。学生原始图片描述随私有草稿保存，系统补充用途相关尺寸和构图要求；公开主页不提供原始提示词。生成结果只更新编辑草稿，保存与发布均为独立操作。

## 本地验证记录

- 独立数据库与本地端口 5189/8089，测试账户和所有演示作品显式标为演示；未修改线上学生作品。
- 浏览器验证新主页保存、重载、发布、停止发布、重发布，以及公开页全屏首图和进入介绍的锚点。共享 renderer 在 390px 预览下无横向溢出。
- 阅读/写作候选只出现已分享作品，未分享演示文章未出现。日历展开真实日期下的四件演示作品，无日期项目单列。
- 真实站内教练调用完成：讨论外部 Codex 网页项目 → 生成两步计划 → 学生确认 → 生成制作规格 → 提交步骤说明 → 进度 50% → 主动点击讨论并发送 → 教练收到说明并继续审核。提交本身不自动把说明发往模型。
- 9 项主页草稿/展示逻辑测试通过；TypeScript 检查与生产构建通过，构建仍有原有大 chunk 提醒。后端完整 API 测试由后端子任务运行通过；步骤确认、修改留存的数据库测试通过。没有建立组件文案类渲染测试。
- 互动课程尚未开发，本轮只保留真实课程入口与教练职责边界。模型行为通过一次本地真实案例验证，不代表系统性教学效果评估。

## 介绍对话、兴趣树与图片公开边界

介绍编辑先呈现对话：从兴趣、在做的事、故事中选择想分享的内容，建议先显示给学生，点击应用后才更新预览；手动修改姓名、简介、关键词和版式保留。对话状态作为私有草稿保存，公开响应不带出。用户已明确授权将主动输入的介绍对话和当前简介经平台后端交给已配置模型处理。介绍对话现已接通 dialogue 档；每次发送仅附带当前姓名、简介、手动关键词、版式和这段对话，不采集兴趣树、来源或其他学习记录。

兴趣树默认不展示，可选择树或关键词。服务端只采集学生已有兴趣词及领域，发布时冻结快照；来源、证据、学习记录、笔记和计数均不公开。手动填写的介绍关键词与兴趣树是两个独立内容来源。

用户明确选择：草稿图片私有，点击发布时才公开。动态图片先进入私有 OSS，保存不会创建公开副本。发布时在行锁下校验版本并复制所选图片到独立公开存储；失败/替换/撤回清理创建的副本，不承诺删除第三方或浏览器已缓存的图片。四套系统 WebP 插画已放入公开 CDN `https://mind-assets.uni-robot.cn/showcase/system/2026-09-22/`。发布后的首图和头像通过该 CDN 的无签名链接访问；草稿通过私有签名链接预览。

公开图片配置独立于私有 OSS：`PUBLIC_ASSET_OSS_ENDPOINT`、`PUBLIC_ASSET_OSS_BUCKET`、`PUBLIC_ASSET_CDN_DOMAIN`、`PUBLIC_ASSET_OSS_ACCESS_KEY_ID`、`PUBLIC_ASSET_OSS_ACCESS_KEY_SECRET`。凭据只在服务端环境，未修改原始 checkout 的环境文件。

本地实测：站内首图模型输出 1664×928，头像输出 1024×1024；原始提示词保存后重载保留；上传 WebP 经站内后端成功；发布后首图/头像均从公开 CDN 加载成功且无签名参数；兴趣树预览和公开页展示明确标为演示的两条兴趣。最终完整 API、PBL、OSS、promptinspect 测试通过。

最后一轮实测：介绍对话完成两次真实模型调用（均有用量记录），先追问一项具体信息，再给出姓名、简介、关键词及照片环绕版式建议；未虚构奖项或经历。点击应用后预览更新，保存并重载后介绍与四条对话保留，公开页仍保持上次发布版本。完整 API/PBL/OSS/promptinspect 测试、主页逻辑测试、前端类型检查及生产构建通过。互动课程仍按约定不在本轮开发范围。
