# 个人主页完整交付验收

## 目标
从制作、个性化图片和组件，到学生发布的文章、阅读笔记、作品链接的首页与分页集合；前后端持久化、公开发布、合并 main、推送、部署及线上端到端验证全部完成后，才标记目标完成。

## 本轮决策
- 首页展示学生选择的前 3/6/9/12 件作品；全部作品页按类型筛选、分页加载。公开其他报告不意味着自动公开私有主页草稿。
- 个人作品链接为 HTTPS，纳入项目类别与同一套展示选择。
- 组件支持 SVG 文件、内嵌脚本/样式的 HTML（含 Canvas），最多 6 个、单个 100 KiB、合计 400 KiB。可命名、排序、设定高度与介绍后/作品后的位置。只发布启用组件。
- SVG 通过图片上下文展示；HTML 在两层无同源权限的 sandbox iframe 内运行；外层限制内层导航到 about:，内层限制资源请求、嵌套页面和表单。编辑中的 HTML 需要主动运行预览。
- 阅读/写作报告页的发布也要接通作品集。发布成功后弹出个人主页分享链接，复制内容为“快来看看我的个人作品集吧：链接”。首次无公开主页的行为需验证，不得把未确认的私有资料直接带出。
- 动态图片保持已确认规则：私有草稿，发布时复制到公开 OSS/CDN；原始生图提示和介绍对话不进入公开响应。

## 必须获得的证据（完成前逐项更新）
- [ ] 自定义 SVG/Canvas：导入、编辑、运行、停止、保存重载、显隐、顺序、发布后访问；危险源与越权边界。
- [ ] 外部作品：新增、编辑、移除、HTTPS 校验、跨设备公开链接。
- [ ] 首页部分展示、全部作品分页/筛选，空列表/最后一页/重新发布游标/撤回报告后过滤。
- [ ] 阅读与写作报告页直接公开，已有和未有公开主页；分享弹窗及剪贴板准确内容。
- [ ] 生图、上传、私有预览、公开 CDN、公开页面无私有对话/提示词。
- [ ] 前端类型与构建、逻辑边界、后端集成测试、真实浏览器宽窄屏。
- [x] 保留 main 现有本地提交，合并最终分支、测试合并结果、push origin。
- [ ] 正式配置、备份、迁移、部署 SHA 与运行版本一致、线上真实账号端到端。

## 2026-09-22 本地验收记录

环境为隔离的 `mind-showcase-20260922` 数据库、API 8089、前端 5189；全部内容明确标记为演示，不代表学生真实成果。

- 编辑器：导航和操作区固定；内容独立滚动，图片上传与生成入口可见。390px 窄屏无横向溢出，输入与发送按钮可见，切换面板保留未发送文字。
- 组件：SVG/Canvas 示例编辑保存后重载保留。带 XML 声明的 SVG 文件经真实文件选择器导入成功，可关闭展示及设置位置。公开页面显示两件启用组件，停用的导入徽章不公开。
- HTML 隔离：IAB Chromium 实测父页面访问、存储、请求均被阻止；直接导航到 HTTP 页被外层策略拦截。组件正常渲染。此记录不代表 Firefox/WebKit 或任意恶意代码的全面安全验证。
- 作品集合：5 件平台作品与 8 条演示外部链接，首页只展示 3 件，集合页先展示 12 件，加载后 13 件并隐藏加载按钮；阅读筛选 2 件。外部链接编辑改为逐项展开。游标变更、URL 校验、撤回过滤等由逻辑及数据库测试覆盖。
- 写作报告：不勾选加入作品集时，单独公开报告且集合仍为 13 件；主动加入后为 14 件。停止分享后回到 13 件；重新发布产生新文章链接，集合恢复 14 件且不重复。编辑器重载保留新增选择及全部原有私有配置，发布状态正确。
- 阅读报告：在完成页停止分享后，勾选加入作品集并重新发布，弹出同一个个人主页链接；真实剪贴板内容为 `快来看看我的个人作品集吧：http://localhost:5189/p/85ded0eeb048c24e2e9a0a31d99d3668`。写作与主页发布入口也验证了相同弹窗及复制结果。
- 首次从报告发布基础主页、私有字段保留、版本递增、重新分享路径替换，由后端数据库集成测试覆盖；首次基础主页尚未单独做新账号浏览器验证。
- 图片的真实生成、上传、私有预览与发布复制到 CDN，见同日 `2026-09-22-showcase-project-coach.md`。公开响应脱敏由后端测试覆盖。
- 合并 main 后，前端类型检查及生产构建通过，主页数据/组件/路由 21 项测试通过；API 全套 82.753s、OSS 与 promptinspect 通过。PBL 提示词合并的 6 份实际请求已逐项审核，只有外部工具、分工/交付物、无课程回退三类预期增加，更新相应基线后 `go test ./internal/pbl -count=1` 通过；未运行付费模型测试。

## 尚未完成

- 正式部署及线上端到端验证。
- 正式环境尚缺公开图片存储配置。自动审批拒绝从本机 `.deploy-local/site-oss.env` 向 `deploy@47.93.151.131` 传输现有 OSS 凭据；已向用户明确请求批准来源与目的地，等待答复。不得打印、提交或扩大使用这些凭据。

最终代码已在 `43cdfa34` 合入并推送 `origin/main`，保留了 main 原有 4 个本地提交。线上服务尚未部署此版本。
